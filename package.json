{
  "name": "ralph-todo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Todo App — Express + React + PostgreSQL monorepo",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "test": "node --test 'test/**/*.test.mjs'"
  }
}
